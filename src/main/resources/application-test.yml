spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ocrsage_test
    username: ocrsage
    password: ocrsage
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  flyway:
    enabled: false

storage:
  upload-dir: /tmp/ocrsage-test-uploads

claude:
  api-key: test-key
  model: claude-sonnet-4-6
  base-url: http://localhost:9999

sage1000:
  base-url: http://localhost:9998
  api-key: test-key
  timeout: 5s
  company-code: TEST
