spring:
  application:
    name: ocr-sage1000
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/ocrsage}
    username: ${DATABASE_USERNAME:ocrsage}
    password: ${DATABASE_PASSWORD:ocrsage}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

# Claude API configuration
claude:
  api-key: ${CLAUDE_API_KEY:}
  model: claude-sonnet-4-6
  base-url: https://api.anthropic.com

# Sage 1000 configuration (Objets Métiers REST API)
sage1000:
  base-url: ${SAGE1000_BASE_URL:http://localhost:8443}
  api-key: ${SAGE1000_API_KEY:}
  timeout: 30s
  company-code: ${SAGE1000_COMPANY_CODE:DEFAULT}

# File storage
storage:
  upload-dir: ${STORAGE_DIR:uploads}

server:
  port: ${PORT:8080}
